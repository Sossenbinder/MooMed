using Autofac;

namespace MooMed.ProfilePictureService.Module
{
    public class ProfilePictureModule : Autofac.Module
    {
        protected override void Load(ContainerBuilder builder)
        {
            base.Load(builder);
        }
    }
}
